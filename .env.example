# Copy this file to .env and fill in your key.
# .env is gitignored — never commit the real key.
#
# NOTE: Chrome extensions cannot read .env files at runtime.
# Paste your Gemini API key into the extension popup instead.
# This file is for developer reference only.

GEMINI_API_KEY=your_gemini_api_key_here
